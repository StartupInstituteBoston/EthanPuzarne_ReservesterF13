<h1>This is a list of restaurants</h1>

<h3>To add a new restaurant, click <%= link_to 'New', new_restaurant_path%></h3>

<ul>
<% @restaurants.each do |r| %>
	<li><%=image_tag r.photo_url(:thumb) %><%= link_to(r.name, r) %></li>
<% end %>
</ul>